import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.batuwa.com',
  appName: 'batuwa',
  webDir: 'www',
  server: {
    androidScheme: 'http'
  }
};

export default config;
